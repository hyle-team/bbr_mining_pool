import { HashPowerConverterPipe } from './hash-power-converter.pipe';

describe('HashPowerConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new HashPowerConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
